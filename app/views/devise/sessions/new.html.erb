<div class="container-fluid text-center">
  <div class='div-with-image'>
    <h1 class='title'>Instarails</h1>
    <h4 class="mt-3">The number two photo social app</h4>
  </div>
  <br />
  <h5 class='mb-4'>Sign in</h5>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <%= f.email_field :email, placeholder: 'email', class: 'input-field', autofocus: true %>
    <%= f.password_field :password, placeholder: 'password', class: 'input-field', autocomplete: "off" %>

    <% if devise_mapping.rememberable? -%>
      <div class='remember'>
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
        </div>
    <% end -%>

    <%= f.submit "Log in", class: 'btn btn-primary' %>
  <% end %>

  <%= render "devise/shared/links" %>
  
  <%= link_to 'Back', root_path, class: 'btn btn-edit' %>
</div>